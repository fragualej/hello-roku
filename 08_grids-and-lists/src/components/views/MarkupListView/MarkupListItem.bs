sub init()
    m.itemRect = m.top.findNode("itemRect")
    m.titleLabel = m.top.findNode("titleLabel")
    m.descLabel = m.top.findNode("descLabel")

    m.baseColor = Constants.Colors.Secondary
    m.focusColor = Constants.Colors.Primary
end sub

sub onContentChanged()
    content = m.top.itemContent
    if content <> invalid
        m.titleLabel.text = content.title

        if content.hasField("description") and content.description <> invalid
            m.descLabel.text = content.description
        end if

        if content.hasField("itemColor") and content.itemColor <> invalid
            m.baseColor = content.itemColor
        end if

        updateColor()
    end if
end sub

sub onFocusChanged()
    updateColor()
end sub

sub updateColor()
    focusPercent = m.top.focusPercent

    if focusPercent > 0.5
        m.itemRect.color = m.focusColor
    else
        m.itemRect.color = m.baseColor
    end if
end sub
